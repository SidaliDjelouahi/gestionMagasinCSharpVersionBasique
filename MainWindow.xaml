<Window x:Class="MonAppGestion.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MonAppGestion"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Loaded="Window_Loaded"
        FocusManager.FocusedElement="{Binding ElementName=txtUsername}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Connexion" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            
            <TextBlock Text="Utilisateur :"/>
            <TextBox x:Name="txtUsername" Margin="0,5,0,15" Padding="5"/>
            
            <TextBlock Text="Mot de passe :"/>
            <PasswordBox x:Name="txtPassword" Margin="0,5,0,15" Padding="5"/>
            
            <Button Content="Se connecter" Height="35" Background="#007ACC" Foreground="White" Click="btnLogin_Click"/>
            
            <TextBlock x:Name="lblMessage" Margin="0,15,0,0" Foreground="Red" HorizontalAlignment="Center"/>
        </StackPanel>

    </Grid>
</Window>
