<Window x:Class="MonAppGestion.NewClientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nouveau client" Height="220" Width="520" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Nom:" Width="80" VerticalAlignment="Center"/>
                <TextBox x:Name="txtNom" Width="380" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Adresse:" Width="80" VerticalAlignment="Center"/>
                <TextBox x:Name="txtAdresse" Width="380" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Téléphone:" Width="80" VerticalAlignment="Center"/>
                <TextBox x:Name="txtTelephone" Width="200" />
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3" Margin="0,12,0,0">
            <Button x:Name="btnCancel" Content="Annuler" Width="100" Margin="0,0,8,0" Click="btnCancel_Click" />
            <Button x:Name="btnOk" Content="Créer" Width="100" Click="btnOk_Click" />
        </StackPanel>
    </Grid>
</Window>
