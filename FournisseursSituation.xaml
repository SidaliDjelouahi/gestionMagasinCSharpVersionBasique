<Window x:Class="MonAppGestion.FournisseursSituation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Situation fournisseur" Height="500" Width="800" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8" HorizontalAlignment="Left" Grid.Row="0">
            <StackPanel Margin="0,0,24,0">
                <TextBlock Text="Total achats" FontWeight="Bold" />
                <TextBlock x:Name="txtTotalAchats" FontSize="16" />
            </StackPanel>
            <StackPanel Margin="0,0,24,0">
                <TextBlock Text="Total versements" FontWeight="Bold" />
                <TextBlock x:Name="txtTotalVersements" FontSize="16" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Reste" FontWeight="Bold" />
                <TextBlock x:Name="txtReste" FontSize="16" />
            </StackPanel>
        </StackPanel>

        <DataGrid x:Name="dgSituation" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="N° Achat" Binding="{Binding NumAchat}" Width="120" />
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=d}" Width="140" />
                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=N2}" Width="120" />
                <DataGridTextColumn Header="Versement" Binding="{Binding Versement, StringFormat=N2}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
