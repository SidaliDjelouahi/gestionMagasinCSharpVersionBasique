<Page x:Class="MonAppGestion.Achats"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Achats"
      Loaded="Page_Loaded">
    <Grid Margin="12">
        <TextBlock Text="Liste des achats" FontSize="20" FontWeight="Bold" />
        <DataGrid x:Name="dgAchats" Margin="0,40,0,0" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="80" />
                <DataGridTextColumn Header="NumAchat" Binding="{Binding NumAchat}" Width="*" />
                <DataGridTextColumn Header="Fournisseur" Binding="{Binding FournisseurName}" Width="200" />
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="150" />
                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=N2}" Width="120" />
                <DataGridTemplateColumn Header="Actions" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Imprimer" Click="Action_Print_Click" Padding="6,2" Margin="0,0,6,0" />
                                <Button Content="Consulter" Click="Action_Consulter_Click" Padding="6,2" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>